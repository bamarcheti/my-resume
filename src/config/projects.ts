import type { ProjectCategory } from "@/types/content";

export const DEFAULT_CATEGORIES: ProjectCategory[] = [
  "Todos",
  "ReactJS",
  "NextJS",
  "React Native",
  "JavaScript",
  "TypeScript",
  "Node.js",
];

// paginação padrão da grade de projetos
export const PROJECTS_PAGE_SIZE = 9;
